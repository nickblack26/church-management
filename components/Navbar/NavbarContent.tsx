import { FiGrid } from 'react-icons/fi';
import NavbarItem from './NavbarItem';

const NavbarContent = () => {
	return (
		<ul>
			<NavbarItem Icon={FiGrid} text='Dashboard' />
		</ul>
	);
};

export default NavbarContent;
