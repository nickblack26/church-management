import NavbarContent from './NavbarContent';
import NavbarHeader from './NavbarHeader';

const Navbar = () => {
	return (
		<nav>
			<NavbarHeader />

			<NavbarContent />
		</nav>
	);
};

export default Navbar;
